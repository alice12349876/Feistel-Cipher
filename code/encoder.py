import random
import bitarray

shift

def generateKey(p):
    pass

    # may need modification
    # num = bin(random.randint(0,2**p - 1))
    # num = num[2:]
    # while len(num) != p:
    #     num = "0" + num
    # keys.append(num)
    # return num
# print(generateKey(4))

# for i in range(16):
def initialPermutation(s):
    bitarray.bitarray()
